services:
  - type: web
    name: dashboard-md-workspace
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONDAY_TOKEN
        sync: false   # se configura manualmente en el dashboard de Render
    autoDeploy: true
